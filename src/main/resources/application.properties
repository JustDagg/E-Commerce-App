server.port=8889
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/e_commerce_app_docker?characterEncoding=UTF-8&serverTimeZone=UTC
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:123456}
# -----------------------
# Naming entity mapping table in SQL
# -----------------------
# spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# -----------------------
# Show sql
# -----------------------
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC


# -----------------------
# Paging
# -----------------------
spring.data.web.pageable.default-page-size=10

spring.data.web.pageable.one-indexed-parameters=true

spring.data.web.pageable.page-parameter=pageNumber

spring.data.web.pageable.size-parameter=size

spring.data.web.sort.sort-parameter=sort

# ---------------------
# Email
# ---------------------
# ---- Config email password ----
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=namphamviet0710@gmail.com
spring.mail.password=l y u t g u v q m k h r r k w h
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#Oauth2 configure(use your id, secret api)
#spring.security.oauth2.client.registration.google.client-id=
#spring.security.oauth2.client.registration.google.client-secret=
